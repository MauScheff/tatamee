- client/ <> Contains all the Java Classes
- public/ <> Contains the CSS & HTML Files used, and Images. In ClientApplication.html it's possible to add HMTL Meta tags,
  When the client code is compiled, everything is embbeded into this file. 
- ClientApplication.gwt.xml <> Includes java paths to libraries, the theme being used, the google maps API
  (Needs to be changed between development and release), the Entry Point class, and CSS file.
- readme.txt <> Myself
